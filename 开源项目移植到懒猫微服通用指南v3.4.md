 # 开源项目移植到懒猫微服通用指南 v3.4

> **版本更新说明 v3.4 (2025-11-27)** - 基于懒猫微服 v1.4.1 更新
> - 🔥 **重要变更**：`services.[].health_check` 字段已废弃，改用 `healthcheck`
> - ✨ 新增 `healthcheck` 字段，与 docker-compose 保持 100% 兼容
> - ✨ 时间单位必须添加后缀（`s`秒、`m`分、`h`小时），如 `10s`、`3m`
> - ✨ `application.health_check` 新增 `timeout` 字段
> - 📝 更新所有健康检查配置示例为新规范
> - ⚠️ 旧字段 `start_period` 的值会被忽略，务必迁移到新格式
>
> **版本更新说明 v3.3 (2025-11-03)** - 基于PandaWiki移植实践经验
> - 🔥 新增"懒猫平台保留服务名"问题（`app`是保留关键字）
> - 🔥 新增"Nginx配置中硬编码服务名"问题和解决方案
> - 🔥 新增"Nginx配置文件冲突"问题（default.conf优先级）
> - 🔥 新增"Nginx端口和SSL配置错误"问题
> - 🔥 新增"NATS JetStream未启用"问题和配置方法
> - 🔥 新增"Command字段格式错误"详细说明
> - 🔥 新增"服务依赖缺失"问题排查方法
> - ✨ 新增完整的"问题排查与日志调试技巧"章节
>   - 查看服务运行状态的方法
>   - 容器日志查看和分析技巧
>   - 进入容器内部调试方法
>   - 诊断常见问题的具体步骤
>   - 完整的排查流程清单
> - 📝 所有新增内容均来自PandaWiki项目的实际移植案例
>
> **版本更新说明 v3.2 (2025-09-09)**
> - 新增"多语言支持配置"章节，详细说明 locales 字段使用方法
> - 添加 BCP 47 语言代码规范说明
> - 提供多语言配置示例和最佳实践
>
> **版本更新说明 v3.1 (2025-09-01)**
> - 修正环境变量名称：`LZC_APP_DOMAIN` → `LAZYCAT_APP_DOMAIN`
> - 新增"懒猫微服环境变量说明"章节，详细说明系统环境变量使用方法
> - 添加环境变量常见错误和解决方案
> - 补充 command 字段格式说明（必须为字符串，不能是数组）

本指南提供了将开源项目移植到懒猫微服平台的通用步骤和最佳实践。基于大量实践经验，特别是 n8n-workflows 等复杂项目的移植案例，整理出核心要点和解决方案。

## 一、准备工作

### 1. 参考文档

```bash
https://developer.lazycat.cloud/spec/manifest.html
https://developer.lazycat.cloud/spec/build.html
https://developer.lazycat.cloud/app-example-python-description.html
```

### 2. 评估项目兼容性

在开始移植前，评估开源项目是否适合在懒猫微服上运行：

- 确认项目已有Docker容器化支持
- 检查项目对外提供的端口和服务
- 确认项目的数据存储需求和挂载点
- 评估项目的资源需求（CPU、内存等）
- **评估网络绑定方式**：检查应用是否绑定到 `127.0.0.1`（需修改）

**注意**：
- PowerShell不支持&&语法，命令要分步执行
- PowerShell的Compress-Archive只支持.zip格式

## 二、懒猫微服环境变量说明（重要！）

### 1. 系统环境变量

懒猫微服会自动注入以下系统环境变量，请确保使用正确的变量名：

| 变量名 | 说明 | 使用示例 |
|--------|------|----------|
| `LAZYCAT_APP_DOMAIN` | 应用的动态域名 | `https://${LAZYCAT_APP_DOMAIN}` |
| `LAZYCAT_APP_ID` | 应用的唯一标识符 | `${LAZYCAT_APP_ID}` |
| `LAZYCAT_BOX_NAME` | 微服名称 | `${LAZYCAT_BOX_NAME}` |

### 2. 常见错误和解决方案

**❌ 错误示例**：
```yaml
environment:
  - BASE_URL=https://${LZC_APP_DOMAIN}  # 错误的变量名
  - NEXTAUTH_URL=https://  # URL不完整
```

**✅ 正确示例**：
```yaml
environment:
  - BASE_URL=https://${LAZYCAT_APP_DOMAIN}  # 正确的变量名
  - NEXTAUTH_URL=https://${LAZYCAT_APP_DOMAIN}
  - ALLOWED_HOSTS=${LAZYCAT_APP_DOMAIN}
```

### 3. 环境变量处理注意事项

1. **变量名前缀**：所有懒猫微服系统变量都以 `LAZYCAT_` 开头，不是 `LZC_`
2. **动态性**：这些变量值可能在容器重启时变化，不要硬编码或永久存储
3. **默认值**：建议为自定义环境变量提供默认值：`${MY_VAR:-default_value}`
4. **命令字段格式**：在 manifest.yml 中，`command` 字段必须是字符串，不能是数组

### 4. 在启动脚本中使用环境变量

如果应用启动脚本没有正确处理环境变量，可以通过自定义命令覆盖：

```yaml
services:
  myapp:
    command: /bin/sh -c "echo 'BASE_URL=https://${LAZYCAT_APP_DOMAIN}' >> .env && ./start.sh"
```

## 三、制作Docker镜像

### 1. 获取开源项目的Docker配置

大多数开源项目会提供官方Docker镜像或docker-compose.yml文件：

```bash
# 克隆项目源码
git clone https://github.com/username/opensource-project.git
cd opensource-project

# 查看Docker配置
cat docker-compose.yml  # 如果存在
cat Dockerfile         # 检查网络绑定配置
```

### 2. 检查网络绑定问题（重要！）

检查 Dockerfile 或启动脚本中的网络绑定：

```dockerfile
# 问题配置 - 只能容器内访问
ENTRYPOINT ["python", "app.py", "--host", "127.0.0.1"]

# 正确配置 - 允许外部访问
ENTRYPOINT ["python", "app.py", "--host", "0.0.0.0", "--port", "8000"]
```

### 3. 准备应用图标

为应用准备一个合适的图标，建议尺寸为256×256像素，格式为PNG：

```bash
# 将现有图标复制并重命名
cp project-logo.png lzc-icon.png
```

## 三、创建docker-compose.yml文件

如果项目已有docker-compose.yml文件，进行必要修改；如果没有，则需创建一个：

```yaml
version: '3.8'

services:
  app-name:
    image: project-image:latest
    container_name: app-name
    restart: unless-stopped
    environment:
      - KEY1=VALUE1
      - KEY2=VALUE2
      - BASE_URL=https://${LAZYCAT_APP_DOMAIN}  # 新增：动态域名支持
    volumes:
      - ./data:/app/data
    ports:
      - 8080:8080
```

**注意事项**：

- 服务名称应简洁明了，避免特殊字符
- 使用相对路径挂载卷
- 明确暴露需要的端口
- 添加 `BASE_URL` 环境变量支持动态域名
- 设置restart策略为unless-stopped

## 四、使用docker2lzc转换为LPK包

### 1. docker2lzc工具安装

如果还没有安装docker2lzc工具：

```bash
# 使用npm安装
npm install -g docker2lzc

# 或使用yarn安装
yarn global add docker2lzc

# 验证安装
docker2lzc --version
```

### 2. 基本转换

使用docker2lzc工具将docker-compose.yml转换为LPK包：

```bash
docker2lzc -n "应用名称" \
  -p "cloud.lazycat.app.appname" \
  -d "应用详细功能描述，突出核心特性和价值" \
  -a "开发者名称" \
  -s "app-subdomain" \
  -i "lzc-icon.png" \
  -c "docker-compose.yml" \
  --background-task false \
  --multi-instance false \
  --non-interactive
```

**参数说明**：

- `-n`: 应用名称（显示名称）
- `-p`: 应用包名（唯一标识符，反向域名格式）
- `-d`: **详细应用描述**（突出功能特点和价值）
- `-a`: 作者名称
- `-s`: 子域名（访问应用的URL前缀）
- `-i`: 图标路径
- `-c`: docker-compose.yml文件路径
- `--background-task`: 是否后台运行
- `--multi-instance`: 是否支持多实例

### 3. 交互式配置重点

在转换过程中，**关键配置选择**：

- **路由类型**：选择HTTP路由
- **路由路径**：设为"/"
- **服务选择**：选择主要的Web服务（避免选择数据库服务）
- **服务端口**：选择Web界面端口（如8000、8080等）
- **目标路径**：设为"/"
- **镜像推送**：建议选择推送到懒猫微服镜像库
- **卷挂载**：只保留必要的数据目录挂载

## 五、复制镜像到懒猫微服镜像库

为确保其他用户安装应用时能正常拉取镜像，需将Docker Hub上的镜像复制到懒猫微服镜像库：

### 1. 批量镜像复制

```bash
# 复制主应用镜像
lzc-cli appstore copy-image author/app:latest

# 复制依赖镜像
lzc-cli appstore copy-image mongo:4.0
lzc-cli appstore copy-image redis:alpine
```

执行该命令后，您将获得一个新的镜像地址，格式为：
`registry.lazycat.cloud/yourname/author/app:哈希值`

## 六、修改LPK包配置（关键步骤）

### 1. 解压LPK包

```bash
# Linux/Mac
mkdir -p lpk-extract && unzip -q cloud.lazycat.app.appname.lpk -d lpk-extract

# Windows PowerShell
Copy-Item cloud.lazycat.app.appname.lpk cloud.lazycat.app.appname.zip
Expand-Archive -Path cloud.lazycat.app.appname.zip -DestinationPath lpk-extract -Force
```

### 2. 修改manifest.yml - 核心配置

编辑manifest.yml文件，重点关注以下配置：

```yaml
name: "应用名称"
package: "cloud.lazycat.app.appname"          # 必需：包名标识
description: "详细功能描述，突出核心价值"  # 重要：详细描述
version: "1.0.0"

# 多语言配置（可选）
locales:
  zh:
    name: "应用中文名称"
    description: "应用中文描述"
  zh_CN:
    name: "应用中文名称"
    description: "应用中文描述"
  en:
    name: "Application Name"
    description: "Application description in English"

application:
  name: "应用名称"
  public_path:
    - /                                 # 公开访问路径
  routes:
    - /=http://服务名:端口               # 关键：使用服务名，不是完整域名
  subdomain: "子域名"                   # 访问子域名
  background_task: true                 # 后台任务标识
unsupported_platforms:
  - ios
  - android
services:
  服务名:
    user: root                          # 避免权限问题
    image: registry.lazycat.cloud/yourname/author/app:哈希值  # 使用复制的镜像
    environment:
      - BASE_URL=https://${LAZYCAT_APP_DOMAIN}  # 动态域名支持
    binds:
      - /lzcapp/var/database:/app/database  # 只绑定需要持久化的目录
```

### 3. 关键配置修复

#### 路由配置修复（重要！）

```yaml
# ❌ 错误 - 会导致DNS解析失败
routes:
  - /=http://doc.cloud.lazycat.app.app.lzcapp:8000

# ✅ 正确 - 使用服务名
routes:
  - /=http://doc:8000
```

#### 网络绑定修复

如果 Dockerfile 中绑定到了 `127.0.0.1`，需要在 manifest.yml 中覆盖：

```yaml
services:
  app-name:
    # 移除可能冲突的配置
    # entrypoint: /bin/sh -c              # 删除这行
    # command: "python app.py"            # 删除这行
    
    # 让 Dockerfile 的 ENTRYPOINT 生效，确保绑定到 0.0.0.0
```

#### 数据持久化优化

```yaml
services:
  app-name:
    binds:
      # 只绑定需要持久化的目录
      - /lzcapp/var/database:/app/database
      # 不要绑定包含内置数据的目录，避免覆盖容器内数据
      # - /lzcapp/var/workflows:/app/workflows  # 如果 Dockerfile 已包含，则删除
```
#### 多平台支持

如果此项目支持移动端访问（页面样式正常、美观），则不用加以下代码，如果不支持移动端，需要加上以下代码

```yaml
unsupported_platforms:
  - ios
  - android
```

### 4. 多语言支持配置

通过配置 `locales` 字段，可以让应用支持多语言显示：

#### 配置规范

| 字段名 | 类型 | 描述 |
|--------|------|------|
| `name` | `string` | 应用名称本地化字段 |
| `description` | `string` | 应用描述本地化字段 |
| `usage` | `string` | 应用的使用须知本地化字段（可选） |

#### 语言代码规范

支持的 language key 规范遵循 [BCP 47 标准](https://en.wikipedia.org/wiki/IETF_language_tag)，常用语言代码：

- `zh`: 中文（通用）
- `zh_CN`: 简体中文
- `zh_TW`: 繁体中文
- `en`: 英文
- `ja`: 日文
- `ko`: 韩文

#### 配置示例

```yaml
locales:
  zh:
    name: "WebLLM 聊天"
    description: "基于WebGPU的浏览器内AI对话系统"
    usage: "请确保浏览器支持WebGPU"
  zh_CN:
    name: "WebLLM 聊天"
    description: "基于WebGPU的浏览器内AI对话系统"
  en:
    name: "WebLLM Chat"
    description: "Browser-based AI conversation system powered by WebGPU"
  ja:
    name: "WebLLM チャット"
    description: "WebGPUベースのブラウザ内AIチャットシステム"
```

**注意事项**：
- 如果不配置 `locales`，应用将使用顶层的 `name` 和 `description` 字段
- 配置 `locales` 后，系统会根据用户的语言设置自动选择对应的文本
- 建议至少提供中文（zh 或 zh_CN）和英文（en）两种语言

### 5. 优化配置（推荐）

根据应用类型添加优化配置：

#### Web应用优化

```yaml
services:
  app-name:
    environment:
      - BASE_URL=https://${LAZYCAT_APP_DOMAIN}
      - ALLOWED_HOSTS=${LAZYCAT_APP_DOMAIN}
      - CORS_ALLOWED_ORIGINS=https://${LAZYCAT_APP_DOMAIN}
```

#### API服务优化

```yaml
application:
  background_task: true           # 后台运行
  routes:
    - /=http://service:8000      # 主界面
    - /api=http://service:8000/api  # API路径
```

#### 数据库应用优化

```yaml
services:
  app-name:
    environment:
      - DB_PATH=/app/data/database.db
    binds:
      - /lzcapp/var/data:/app/data
```

### 5. LPK包体积优化

删除不必要的文件以减少包体积：

```bash
# 进入解压目录
cd lpk-extract

# 删除源代码包（通常是最大的文件）
rm -f content.tar

# 只保留必要文件
ls -la  # 查看文件，只保留 manifest.yml 和 icon.png
```

### 6. 重新打包

```bash
# Linux/Mac
cd lpk-extract && zip -r ../app-optimized.lpk manifest.yml icon.png && cd ..

# Windows PowerShell
cd lpk-extract
Compress-Archive -Path manifest.yml,icon.png -DestinationPath ../app-optimized.zip -Force
cd ..
Move-Item app-optimized.zip app-optimized.lpk
```

**优化效果**：包体积可从几十MB减少到几KB，优化率达99%以上。

## 七、安装测试

### 1. 安装应用

```bash
lzc-cli app install app-optimized.lpk
```

### 2. 检查应用状态

```bash
lzc-cli app status your-package
```

### 3. 查看应用日志

```bash
lzc-cli app log your-package
```

## 八、常见问题与解决方案（重点关注）

### 0. 懒猫平台保留服务名 🔥🔥🔥

**症状**：
```
"app" is not allowed to be service name
```

**根本原因**：`app` 是懒猫平台的保留服务名，不能用于自定义服务

**解决方案**：
```yaml
# ❌ 错误配置
services:
  app:  # app是保留关键字
    image: myimage:latest

# ✅ 正确配置
services:
  wiki-frontend:  # 使用具体的服务名
    image: myimage:latest
```

**其他保留关键字**：建议避免使用过于通用的服务名，如 `service`、`main`、`server` 等

### 1. 域名解析错误 🔥

**症状**：
```
lookup doc.com.xxx.lzcapp on 127.0.0.11:53: server misbehaving
lzc域名 "xxx" 未注册成功
```

**根本原因**：路由配置使用了完整域名而非服务名

**解决方案**：
```yaml
# ❌ 错误配置
routes:
  - /=http://doc.cloud.lazycat.app.app.lzcapp:8000

# ✅ 正确配置
routes:
  - /=http://doc:8000
```

### 2. 连接被拒绝错误 🔥

**症状**：
```
dial tcp 172.28.6.3:8000: connect: connection refused
```

**根本原因**：服务绑定到 `127.0.0.1` 而非 `0.0.0.0`

**解决方案**：
1. 检查 Dockerfile 中的网络绑定配置
2. 移除 manifest.yml 中的 `entrypoint` 和 `command` 冲突配置
3. 确保服务启动时绑定到 `0.0.0.0:端口`

### 3. 数据丢失问题 🔥

**症状**：应用启动后显示 0 数据、空白界面

**根本原因**：卷绑定覆盖了容器内置的数据

**解决方案**：
```yaml
services:
  app-name:
    binds:
      # 只保留需要持久化的目录
      - /lzcapp/var/database:/app/database
      # 移除包含内置数据的目录绑定
      # - /lzcapp/var/workflows:/app/workflows  # 删除此行
```

### 4. 镜像拉取失败

**症状**：安装后应用无法启动，日志显示镜像拉取失败

**解决方案**：
- 确认已经使用`lzc-cli appstore copy-image`命令复制镜像
- 确认manifest.yml中使用了正确的懒猫微服镜像地址

### 5. 权限问题

**症状**：应用无法写入文件，报权限错误

**解决方案**：
```yaml
services:
  app-name:
    user: root  # 设置为root用户避免权限问题
    binds:
      - /lzcapp/var/data:/app/data
```

### 6. Nginx配置中硬编码服务名 🔥

**症状**：
```
host not found in upstream "panda-wiki-api:8000"
```

**根本原因**：Nginx配置文件中硬编码了原项目的服务名，而manifest.yml中使用的是简化的服务名

**解决方案**：
修改Nginx配置文件（如 `server.conf`），将硬编码的服务名改为通用名：

```nginx
# ❌ 错误配置 - 硬编码原项目服务名
upstream backend {
    server panda-wiki-api:8000;  # 硬编码的服务名
}

location ~ ^/static-file/ {
    proxy_pass http://panda-wiki-minio:9000;  # 硬编码的MinIO服务名
}

# ✅ 正确配置 - 使用manifest.yml中的服务名
upstream backend {
    server api:8000;  # 与manifest.yml中的服务名一致
}

location ~ ^/static-file/ {
    proxy_pass http://minio:9000;  # 与manifest.yml中的服务名一致
}
```

**检查方法**：
```bash
# 搜索Nginx配置中的硬编码服务名
grep -r "panda-wiki\|project-name" web/admin/
```

### 7. Nginx配置文件冲突 🔥

**症状**：访问应用显示Nginx默认欢迎页面 "Welcome to nginx!"

**根本原因**：Nginx镜像自带 `default.conf`，与自定义配置文件冲突，且default.conf优先级更高

**解决方案**：

在Dockerfile中直接覆盖default.conf：

```dockerfile
# ❌ 错误 - 两个配置文件会冲突
FROM nginx:alpine
COPY server.conf /etc/nginx/conf.d/server.conf  # 会被default.conf覆盖

# ✅ 正确 - 直接替换默认配置
FROM nginx:alpine
COPY server.conf /etc/nginx/conf.d/default.conf  # 替换默认配置
```

**验证方法**：
```bash
# 检查容器内的配置文件
docker exec <container> ls -la /etc/nginx/conf.d/
```

### 8. Nginx端口和SSL配置错误 🔥

**症状**：容器运行但无法访问，或显示SSL错误

**根本原因**：Nginx配置监听了错误的端口或要求SSL，但懒猫平台内部不需要SSL

**解决方案**：

修改Nginx server配置：

```nginx
# ❌ 错误配置 - 监听8080且要求SSL
server {
    listen 8080 ssl http2;
    ssl_certificate /etc/nginx/ssl/cert.crt;
    ssl_certificate_key /etc/nginx/ssl/cert.key;
    ...
}

# ✅ 正确配置 - 监听80端口，无SSL
server {
    listen 80;
    ...
}
```

**注意**：
- 懒猫平台统一在外层处理SSL，容器内部使用HTTP即可
- 容器暴露的端口要与Nginx监听端口一致

### 9. 应用描述不够详细

**症状**：用户不了解应用功能，安装率低

**解决方案**：
```yaml
description: "高性能n8n工作流文档系统 - 包含2053个精选工作流，涵盖AI自动化、业务流程、数据处理等15大类别，提供毫秒级搜索和可视化文档浏览。支持全文检索、分类筛选和工作流导入，是n8n自动化开发的完整解决方案。"
```

突出：功能特点、数据规模、应用场景、核心价值

### 10. NATS JetStream未启用 🔥

**症状**：
```
panic: failed to ensure streams: failed to create stream task:
nats: no responders available for request
```

**根本原因**：NATS消息队列需要启用JetStream功能，但启动命令中缺少 `-js` 参数

**解决方案**：

```yaml
# ❌ 错误配置 - 未启用JetStream
services:
  nats:
    image: nats:2-alpine
    command: "--user myuser --pass mypass"  # 缺少-js参数

# ✅ 正确配置 - 启用JetStream
services:
  nats:
    image: nats:2-alpine
    command: "-js --user myuser --pass mypass"  # 添加-js启用JetStream
```

**注意**：
- JetStream是NATS的持久化消息流功能
- 如果应用使用JetStream API，必须在启动时启用
- command字段必须是字符串，不能是数组

### 11. Command字段格式错误 🔥

**症状**：服务无法启动或行为异常

**根本原因**：manifest.yml中的 `command` 字段使用了数组格式，但懒猫平台只支持字符串格式

**解决方案**：

```yaml
# ❌ 错误配置 - 数组格式
services:
  myapp:
    command:
      - "--flag1"
      - "value1"
      - "--flag2"

# ✅ 正确配置 - 字符串格式
services:
  myapp:
    command: "--flag1 value1 --flag2"
```

**特殊情况处理**：

```yaml
# 包含引号的命令
services:
  myapp:
    command: "sh -c 'echo \"Starting app...\" && ./start.sh'"

# 多行命令（使用shell -c）
services:
  myapp:
    command: "/bin/sh -c \"sleep 10 && ./app\""
```

### 12. 服务依赖缺失 🔥

**症状**：
```
panic: dial tcp <ip>:4222: i/o timeout
panic: connection refused
```

**根本原因**：manifest.yml中遗漏了必需的依赖服务（如数据库、消息队列、对象存储等）

**检查方法**：

1. 查看原项目的docker-compose.yml，确认所有服务
2. 检查应用代码中的连接配置

```bash
# 搜索代码中的服务连接
grep -r "NATS_SERVER\|REDIS_ADDR\|S3_ENDPOINT" backend/
```

**解决方案**：

确保所有依赖服务都添加到manifest.yml中：

```yaml
services:
  # 主应用
  api:
    depends_on:
      - postgres    # 数据库
      - redis       # 缓存
      - nats        # 消息队列
      - minio       # 对象存储
    environment:
      - PG_DSN=host=postgres user=myuser password=mypass dbname=mydb
      - REDIS_ADDR=redis:6379
      - MQ_NATS_SERVER=nats://nats:4222
      - S3_ENDPOINT=minio:9000

  # 依赖服务
  postgres:
    image: postgres:15-alpine

  redis:
    image: redis:alpine

  nats:
    image: nats:2-alpine
    command: "-js --user myuser --pass mypass"

  minio:
    image: minio/minio:latest
    command: "server /data --console-address :9001"
```

### 13. 健康检查配置错误 🔥

**症状**：
- 容器一直处于 `(health: starting)` 状态
- 容器显示 `(unhealthy)` 状态
- 健康检查配置不生效

**根本原因**：使用了废弃的 `health_check` 字段或时间值没有单位

**解决方案**：

```yaml
# ❌ 错误配置 - 使用废弃字段，时间没有单位
services:
  myapp:
    health_check:         # 废弃字段
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8080/health"]
      start_period: 60    # 没有单位，会被忽略
      timeout: 10         # 没有单位，会被忽略

# ✅ 正确配置 - 使用新字段，添加时间单位
services:
  myapp:
    healthcheck:          # 新字段（无下划线）
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8080/health"]
      interval: 30s       # 检查间隔（必须带单位）
      timeout: 10s        # 超时时间（必须带单位）
      retries: 3          # 重试次数
      start_period: 60s   # 启动宽限期（必须带单位）
      start_interval: 5s  # 启动阶段检查间隔（可选）
```

**时间单位说明**：
- `s`：秒（seconds），如 `10s`、`30s`
- `m`：分（minutes），如 `5m`
- `h`：小时（hours），如 `2h`

**迁移检查清单**：
- [ ] 将 `health_check` 改为 `healthcheck`
- [ ] 为所有时间值添加单位后缀
- [ ] 添加 `interval` 字段（推荐 30s）
- [ ] 添加 `timeout` 字段（推荐 10s）
- [ ] 添加 `retries` 字段（推荐 3）
- [ ] 确保 `start_period` 有足够的时间（根据应用启动时间调整）

**参考文档**：
- [懒猫微服健康检查规范](https://developer.lazycat.cloud/spec/manifest.html#_5-2-healthcheckconfig)
- [Docker Compose Healthcheck 规范](https://docs.docker.com/compose/compose-file/compose-file-v3/#healthcheck)

### 14. setup_script 脚本兼容性问题 🔥

**症状**：
```
等待数据库... (1/60)
等待数据库... (2/60)
...
等待数据库... (60/60)
```
数据库日志已显示 "database system is ready to accept connections"，但脚本仍一直重试直到超时

**根本原因**：使用了 `/dev/tcp/host/port` 语法检测端口，但这是 **bash 特有特性**，Alpine 镜像默认使用的是 **sh (busybox shell)**，不支持此语法

**问题分析**：

大多数 Node.js、Python 等应用的官方镜像基于 Alpine Linux，使用的是 sh shell，而非 bash：

| 镜像类型 | 默认 Shell | 支持 /dev/tcp | 推荐检测命令 |
|---------|-----------|--------------|-------------|
| node:alpine | sh | ❌ | `nc -z host port` |
| python:alpine | sh | ❌ | `nc -z host port` |
| nginx:alpine | sh | ❌ | `nc -z host port` |
| debian/ubuntu | bash | ✅ | `nc -z` 或 `/dev/tcp` |

**错误示例**：

```yaml
services:
  app:
    setup_script: |
      echo "等待数据库启动中..."
      for i in $(seq 1 60); do
        # ❌ 错误：Alpine/sh 不支持 /dev/tcp
        (echo > /dev/tcp/postgres/5432) >/dev/null 2>&1 && break
        echo "等待数据库... ($i/60)"
        sleep 2
      done
      echo "数据库已就绪，启动应用..."
```

**正确解决方案**：

```yaml
services:
  app:
    setup_script: |
      echo "等待数据库启动中..."
      for i in $(seq 1 60); do
        # ✅ 正确：使用 nc 命令（Alpine 自带）
        nc -z postgres 5432 >/dev/null 2>&1 && break
        echo "等待数据库... ($i/60)"
        sleep 2
      done
      echo "数据库已就绪，启动应用..."
```

**多种端口检测方法对比**：

```yaml
# 方法1：nc -z（推荐 - 兼容性最好）
nc -z postgres 5432 >/dev/null 2>&1 && echo "OK"

# 方法2：nc 连接测试
timeout 1 nc postgres 5432 </dev/null >/dev/null 2>&1 && echo "OK"

# 方法3：wget/curl（适用于 HTTP 服务）
wget --spider --timeout=1 http://app:8080/health >/dev/null 2>&1 && echo "OK"

# 方法4：telnet（需要安装 telnet 包）
timeout 1 telnet postgres 5432 >/dev/null 2>&1 && echo "OK"

# 方法5：/dev/tcp（仅 bash 支持，不推荐）
(echo > /dev/tcp/postgres/5432) >/dev/null 2>&1 && echo "OK"
```

**完整的数据库等待脚本示例**：

```yaml
services:
  shopping-list:
    depends_on:
      - shopping-list-db
    setup_script: |
      echo "等待数据库启动中..."
      for i in $(seq 1 60); do
        nc -z shopping-list-db 5432 >/dev/null 2>&1 && break
        echo "等待数据库... ($i/60)"
        sleep 2
      done

      if ! nc -z shopping-list-db 5432 >/dev/null 2>&1; then
        echo "错误：数据库在 120 秒后仍未就绪"
        exit 1
      fi

      echo "数据库已就绪，启动应用..."

  shopping-list-db:
    image: postgres:15-alpine
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 30s
      timeout: 20s
      retries: 10
      start_period: 60s
```

**调试技巧**：

```bash
# 1. 检查容器内的 shell 类型
lzc-cli docker exec <container> sh -c "echo \$0"
# 输出 sh 或 bash

# 2. 测试 /dev/tcp 是否支持
lzc-cli docker exec <container> sh -c "(echo > /dev/tcp/8.8.8.8/53) 2>&1"
# 如果不支持会报错：sh: can't create /dev/tcp/8.8.8.8/53: nonexistent directory

# 3. 验证 nc 命令可用性
lzc-cli docker exec <container> which nc
lzc-cli docker exec <container> nc -h

# 4. 手动测试端口连接
lzc-cli docker exec <container> nc -z postgres 5432 && echo "OK" || echo "FAIL"
```

**最佳实践**：

1. **优先使用 `nc -z`**：兼容性最好，Alpine/Debian/Ubuntu 都支持
2. **检查返回值**：脚本末尾检查服务是否真的就绪，失败则 `exit 1`
3. **合理设置超时**：根据依赖服务启动时间调整循环次数和间隔
4. **记录日志**：输出清晰的等待和成功/失败信息
5. **配合健康检查**：依赖服务也要配置 `healthcheck`，确保真正就绪

**相关问题**：
- 问题 12：服务依赖缺失
- 问题 13：健康检查配置错误

## 九、问题排查与日志调试技巧

### 1. 查看服务运行状态

```bash
# 查看所有PandaWiki相关容器
lzc-cli docker ps | grep pandawiki

# 查看所有容器（包括已停止的）
lzc-cli docker ps -a | grep pandawiki
```

**重点关注**：
- `STATUS` 列：容器是否在运行
- `PORTS` 列：端口映射是否正确
- 健康状态：`(healthy)` / `(unhealthy)` / `(health: starting)`

### 2. 查看容器日志

```bash
# 查看特定容器的日志（最后50行）
lzc-cli docker logs <container-name> 2>&1 | tail -50

# 实时查看日志
lzc-cli docker logs -f <container-name>

# 查看API服务日志示例
lzc-cli docker logs cloudlazycatapppandawiki-api-1 2>&1 | tail -100
```

**日志分析技巧**：

1. **查找错误关键词**：
```bash
# 查找panic错误
lzc-cli docker logs <container> 2>&1 | grep -i "panic\|error\|fatal"

# 查找连接问题
lzc-cli docker logs <container> 2>&1 | grep -i "connection\|timeout\|refused"
```

2. **检查启动顺序**：
```bash
# 查看容器创建时间
lzc-cli docker ps -a --format "table {{.Names}}\t{{.Status}}\t{{.CreatedAt}}"
```

### 3. 进入容器内部调试

```bash
# 进入容器shell
lzc-cli docker exec -it <container-name> sh

# 在容器内检查进程
lzc-cli docker exec <container> ps aux

# 检查网络连接（使用 nc 命令）
lzc-cli docker exec <container> nc -z postgres 5432 && echo 'OK' || echo 'FAIL'

# 检查配置文件
lzc-cli docker exec <container> cat /etc/nginx/conf.d/default.conf
```

### 4. 诊断常见问题

#### API服务卡在setup_script

**症状**：API容器运行但主进程未启动

**检查方法**：
```bash
# 查看容器进程
lzc-cli docker exec <api-container> ps aux
# 如果看到一直在执行 sleep 2，说明卡在等待依赖
```

**解决方案**：
```yaml
# 简化setup_script，减少等待时间
setup_script: |
  echo "Waiting for dependencies..."
  for i in $(seq 1 10); do  # 减少到10次重试
    nc -z postgres 5432 >/dev/null 2>&1 && break  # 使用 nc 而非 /dev/tcp
    sleep 1  # 减少到1秒
  done
```

#### Consumer服务退出

**症状**：Consumer容器状态为 `Exited (2)`

**检查方法**：
```bash
# 查看退出日志
lzc-cli docker logs <consumer-container> 2>&1

# 常见错误：
# - "nats: no stream matches subject" → NATS JetStream未启用
# - "connection refused" → 依赖服务未就绪
```

#### 健康检查失败

**症状**：容器状态显示 `(unhealthy)`

**检查方法**：
```bash
# 查看健康检查配置
lzc-cli docker inspect <container> | grep -A 10 "Health"

# 手动执行健康检查命令
lzc-cli docker exec <container> nc -z localhost 4222
```

**修复示例（新版格式）**：
```yaml
healthcheck:  # 新版：使用 healthcheck（无下划线）
  test:
    - CMD
    - sh
    - -c
    - "nc -z 127.0.0.1 4222 || exit 1"  # 确保命令正确
  interval: 30s      # 检查间隔（必须带单位）
  timeout: 3s        # 超时时间（必须带单位）
  retries: 5         # 重试次数
  start_period: 10s  # 启动宽限期（必须带单位）
```

**时间单位说明**：
- 所有时间值必须带单位：`s`（秒）、`m`（分）、`h`（小时）
- 旧版不带单位的配置（如 `start_period: 10`）会被忽略

### 5. 镜像问题排查

```bash
# 查看已上传的镜像
lzc-cli appstore my-images | grep pandawiki

# 验证镜像hash是否匹配manifest.yml
cat lpk-extract/manifest.yml | grep "image:"
```

### 6. 完整排查流程

遇到问题时，按以下顺序排查：

1. **检查容器状态** → `lzc-cli docker ps -a`
2. **查看失败容器日志** → `lzc-cli docker logs <container>`
3. **检查依赖服务** → 确认postgres、redis、nats等是否healthy
4. **验证网络连接** → exec进容器测试服务间通信
5. **检查配置文件** → 验证manifest.yml和应用配置
6. **重新构建镜像** → 如果是镜像问题，修复后重新构建

## 十、高级配置技巧

### 1. 健康检查配置（重要更新！）

#### 新版健康检查配置（v1.4.1+）

**重要变更**：从懒猫微服 v1.4.1 开始，健康检查字段已更新：
- ✅ **推荐使用**：`healthcheck`（与 docker-compose 100% 兼容）
- ❌ **已废弃**：`health_check`（下划线版本）

**新版配置示例**：

```yaml
services:
  wordpress:
    image: bitnami/wordpress:5.8.2
    healthcheck:
      test:
        - CMD-SHELL
        - "curl -f http://127.0.0.1:80/ || exit 1"
      interval: 30s      # 检查间隔（必须带单位）
      timeout: 10s       # 超时时间（必须带单位）
      retries: 3         # 重试次数
      start_period: 190s # 启动宽限期（必须带单位）
      start_interval: 5s # 启动阶段检查间隔（可选）
```

**时间单位规范**：
- `s`：秒（seconds），如 `10s`、`30s`
- `m`：分（minutes），如 `5m`
- `h`：小时（hours），如 `2h`
- **注意**：旧版本中不带单位的数字（如 `190`）在新版本中会被忽略

#### 兼容性说明

**迁移步骤**：

```yaml
# ❌ 旧版配置（v1.4.1之前）- 已废弃
services:
  myapp:
    health_check:
      test:
        - CMD
        - curl
        - -f
        - http://127.0.0.1:8080/health
      start_period: 60  # 没有单位，会被忽略

# ✅ 新版配置（v1.4.1+）- 推荐使用
services:
  myapp:
    healthcheck:  # 字段名改为 healthcheck（无下划线）
      test:
        - CMD
        - curl
        - -f
        - http://127.0.0.1:8080/health
      interval: 30s      # 新增：检查间隔
      timeout: 10s       # 新增：超时时间
      retries: 3         # 新增：重试次数
      start_period: 60s  # 必须添加单位后缀
      start_interval: 5s # 可选：启动阶段的检查间隔
```

**兼容性处理**：
- 旧版本 LPK 包中的 `health_check.start_period`（不带单位）会被**静默忽略**
- 建议尽快迁移到新版 `healthcheck` 格式
- 即使未配置健康检查，也会受 Docker 镜像中 HEALTHCHECK 指令的影响

#### 常见健康检查配置

**HTTP 服务检查**：
```yaml
healthcheck:
  test: ["CMD-SHELL", "curl -f http://127.0.0.1:8080/health || exit 1"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 40s
```

**TCP 端口检查**：
```yaml
healthcheck:
  test: ["CMD", "nc", "-z", "127.0.0.1", "4222"]
  interval: 30s
  timeout: 3s
  retries: 5
  start_period: 10s
```

**数据库检查**：
```yaml
healthcheck:
  test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1"]
  interval: 30s
  timeout: 20s
  retries: 10
  start_period: 60s
```

**使用 wget 检查**：
```yaml
healthcheck:
  test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8080/health"]
  interval: 30s
  timeout: 5s
  retries: 3
  start_period: 10s
```

**禁用健康检查**：
```yaml
healthcheck:
  disable: true  # 完全禁用健康检查
```

#### Application 级别健康检查

从 v1.4.1 开始，`application.health_check` 新增 `timeout` 字段：

```yaml
application:
  health_check:
    test_url: /health      # 健康检查URL
    timeout: 30s           # 新增：超时时间（必须带单位）
    interval: 60s          # 检查间隔
```

**注意事项**：
1. **字段名变化**：`health_check` → `healthcheck`（服务级别）
2. **时间单位**：所有时间值必须带单位后缀（`s`、`m`、`h`）
3. **向后兼容**：旧字段仍可识别，但会在未来版本中移除
4. **默认值**：如未配置，app 服务会被强制设置为 `start_period=300s, start_interval=1s`

### 2. 后台任务模式

对于无UI界面的服务类应用：

```yaml
application:
  background_task: true

services:
  service-name:
    restart: unless-stopped
```

### 3. 复杂路由配置

为复杂应用配置多路径路由：

```yaml
application:
  routes:
    - /=http://main-service:8080/
    - /api=http://main-service:8080/api
    - /static=http://main-service:8080/static
  public_path: "/"
```

### 4. 多服务协作配置

包含多个服务的复杂应用：

```yaml
services:
  frontend:
    image: app-frontend:latest
    environment:
      - API_URL=http://backend:8080
  
  backend:
    image: app-backend:latest
    environment:
      - DB_URL=http://database:5432
  
  database:
    image: postgres:15
    binds:
      - /lzcapp/var/db:/var/lib/postgresql/data
```

### 5. 环境变量最佳实践

基于懒猫微服环境特点：

```yaml
services:
  app-name:
    environment:
      # 基础配置
      - APP_NAME=${LAZYCAT_APP_NAME}
      - SECRET_KEY=${LAZYCAT_BOX_NAME}
      
      # 动态域名配置
      - BASE_URL=https://${LAZYCAT_APP_DOMAIN}
      - ALLOWED_HOSTS=${LAZYCAT_APP_DOMAIN}
      
      # 数据路径配置
      - DATA_DIR=/app/data
      - CACHE_DIR=/app/cache
      
      # 性能优化
      - THREADS=4
      - WORKERS=2
```

## 十、移植质量检查清单

### 1. 配置验证
- [ ] 应用信息配置完整（name、package、description）
- [ ] 路由使用服务名而非完整域名（`服务名:端口`）
- [ ] 服务绑定到 0.0.0.0（检查网络绑定）
- [ ] 卷绑定策略合理（只绑定需要持久化的目录）
- [ ] 避免 Dockerfile 与 manifest 冲突
- [ ] 环境变量配置正确（包含动态域名支持）
- [ ] 镜像地址已更新为懒猫微服镜像库

### 2. 功能验证
- [ ] 应用能正常启动
- [ ] Web 界面可以访问
- [ ] 核心功能正常工作
- [ ] 数据持久化正常
- [ ] 用户认证正常（如适用）

### 3. 性能验证
- [ ] 应用启动时间合理
- [ ] 响应速度正常
- [ ] 内存使用合理
- [ ] 包体积已优化（删除不必要文件）

### 4. 安全验证
- [ ] 不必要的端口未暴露
- [ ] 敏感信息未硬编码
- [ ] 权限配置合理
- [ ] 数据访问受限

## 十一、成功案例参考

### BookHaven 项目移植案例

**最终工作配置**：

```yaml
package: cloud.lazycat.app.bookhaven
version: 1.2.3
name: BookHaven
description: 现代化的浏览器端 EPUB 阅读器
usage: 请把对应书籍放在懒猫网盘目录下的book文件夹内（如无，请新建一下个）。首次启动较慢，需要等待5-6分钟后刷新页面即可进入。
author: HrBingR
homepage: https://github.com/HrBingR/BookHaven
locales:
  zh:
    name: BookHaven
    description: 现代化的浏览器端 EPUB 阅读器，支持本地文件管理、元数据编辑、搜索、认证等功能。
  en:
    name: BookHaven
    description: >-
      A modern browser-based EPUB reader with local file management, metadata
      editing, search, and authentication.
application:
  subdomain: bookhaven
  multi_instance: true
  background_task: false
  routes:
    - /=http://bookhaven:5000/
  public_path:
    - /manifest.json
    - /icon.svg
    - /static/
    - /assets/
  ingress:
    - protocol: tcp
      port: 5000
      service: bookhaven
services:
  mysql:
    image: registry.lazycat.cloud/edward/library/mysql:f60cc6d302503aea
    environment:
      - MYSQL_ROOT_PASSWORD=Admin123!
      - MYSQL_DATABASE=epub_library
      - MYSQL_USER=epub_user
      - MYSQL_PASSWORD=Admin123!
    binds:
      - /lzcapp/var/mysql:/var/lib/mysql
    healthcheck:  # 新版：使用 healthcheck（无下划线）
      test:
        - CMD
        - mysqladmin
        - ping
        - '-h'
        - localhost
      interval: 30s       # 检查间隔（必须带单位）
      timeout: 20s        # 超时时间
      retries: 10         # 重试次数
      start_period: 360s  # 启动宽限期（必须带单位）
  redis:
    image: registry.lazycat.cloud/edward/library/redis:630b4b80b5d10832
    binds:
      - /lzcapp/var/redis:/data
  bookhaven:
    image: registry.lazycat.cloud/edward/hrbingr/bookhaven:e514f55291d5e941
    environment:
      - BASE_DIRECTORY=/ebooks
      - BASE_URL=https://${LAZYCAT_APP_DOMAIN}
      - >-
        SECRET_KEY=d6e3781e96405fe946f55a94619dbca794e392a2fdd3c72180f2398a7a7c6b8c
      - ADMIN_EMAIL=admin@lazycat.com
      - ADMIN_PASS=Admin123!
      - ADMIN_RESET=true
      - LOG_LEVEL=INFO
      - APP_PORT=5000
      - DB_TYPE=mysql
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_NAME=epub_library
      - DB_USER=epub_user
      - DB_PASSWORD=Admin123!
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_LIMITER_DB=0
      - REDIS_SCHEDULER_DB=5
      - REDIS_LOCK_DB=6
      - UPLOADS_ENABLED=true
      - UPLOADS_DIRECTORY=/uploads
      - SCHEDULER_ENABLED=true
      - PERIODIC_SCAN_INTERVAL=10
    binds:
      - /lzcapp/run/mnt/home/book:/ebooks
      - /lzcapp/var/uploads:/uploads
    depends_on:
      - mysql
      - redis
    setup_script: >
      echo "Waiting for Redis at redis:6379..."

      for i in $(seq 1 30); do
        nc -z redis 6379 >/dev/null 2>&1 && break
        sleep 2
      done

      echo "Waiting for MySQL at mysql:3306..."

      for i in $(seq 1 90); do
        nc -z mysql 3306 >/dev/null 2>&1 && break
        sleep 2
      done

      mysqladmin ping -hmysql -uepub_user -p"${DB_PASSWORD}" >/dev/null 2>&1 ||
      echo "mysqladmin ping failed, continuing..."

      echo "Dependencies reachable. Starting app."
```

**注意**：以上配置已更新为使用 `nc -z` 命令而非 `/dev/tcp`，确保在 Alpine 镜像中正常工作。详见"问题14：setup_script 脚本兼容性问题"。

**关键解决方案**：
1. **路由配置**：使用 `doc:8000` 而非完整域名
2. **网络绑定**：Dockerfile 配置 `--host 0.0.0.0`
3. **数据策略**：只绑定数据库目录，保留容器内的工作流数据
4. **镜像处理**：推送到懒猫微服镜像库

## 十二、最佳实践总结

### 核心原则
1. **简化路由**：使用服务名而非完整域名
2. **网络开放**：服务必须绑定到 0.0.0.0
3. **数据分离**：区分内置数据和需持久化数据
4. **配置清晰**：避免 Dockerfile 与 manifest 配置冲突
5. **描述详细**：应用描述要突出核心价值和功能特点

### 配置优先级
1. **Dockerfile 优先**：优先使用 Dockerfile 中的 ENTRYPOINT
2. **数据策略**：容器内置数据 + 选择性持久化
3. **网络配置**：服务名通信 + 0.0.0.0 绑定
4. **安全考虑**：root 用户 + 合理的权限配置

### 调试方法
1. 检查路由配置是否使用服务名
2. 确认服务绑定地址（0.0.0.0 vs 127.0.0.1）
3. 验证卷绑定是否合理
4. 查看容器日志定位具体问题
5. 测试镜像是否能正确拉取

### 包体积优化
- 删除 `content.tar` 源代码包
- 只保留 `manifest.yml` 和 `icon.png`
- 优化率可达 99% 以上

遵循以上指南，您可以将几乎任何开源项目成功移植到懒猫微服平台，实现生产级的稳定性和性能。特别注意路由配置、网络绑定和数据持久化策略，这是移植成功的关键要素。

---

## 文档信息

- **版本**: v3.4
- **更新日期**: 2025-11-27
- **维护者**: 天天
- **适用平台**: 懒猫微服 (Lazycat Microservice) v1.4.1+
- **实践案例**: PandaWiki、BookHaven、n8n-workflows、Audiarr 等
- **相关文档**:
  - [懒猫微服开发文档](https://developer.lazycat.cloud/)
  - [懒猫微服高级环境变量](https://developer.lazycat.cloud/advanced-envs.html)
  - [Manifest 规范](https://developer.lazycat.cloud/spec/manifest.html)
  - [健康检查配置](https://developer.lazycat.cloud/spec/manifest.html#_5-2-healthcheckconfig)
  - [v1.4.1 更新日志](https://developer.lazycat.cloud/changelogs/v1.4.1.html)
