# 爱心宠物领养平台 - 懒猫微服移植计划

> **版本**: 1.0
> **创建日期**: 2025-12-31
> **参考文档**: 开源项目移植到懒猫微服通用指南 v3.4

---

## 一、项目分析

### 1.1 项目概述

| 项目 | 说明 |
|------|------|
| **项目名称** | 爱心宠物领养平台 (Pet Adoption Platform) |
| **技术栈** | Go + Gin (后端) / React + Vite (前端) |
| **数据库** | MySQL 8.0+ |
| **缓存** | Redis 7.0+ |
| **后端端口** | 8080 |
| **前端端口** | 3000 (开发) / 80 (生产) |

### 1.2 架构分析

```
┌─────────────────────────────────────────────────────┐
│                   懒猫微服平台                        │
│  ┌───────────────────────────────────────────────┐  │
│  │              Nginx (反向代理)                  │  │
│  │   /  → frontend:80                            │  │
│  │   /api → backend:8080                         │  │
│  └───────────────────────────────────────────────┘  │
│           │                    │                    │
│  ┌────────▼───────┐   ┌───────▼────────┐           │
│  │   Frontend     │   │    Backend     │           │
│  │ (React+Nginx)  │   │   (Go+Gin)     │           │
│  │    :80         │   │    :8080       │           │
│  └────────────────┘   └───────┬────────┘           │
│                               │                    │
│              ┌────────────────┼────────────────┐   │
│              │                │                │   │
│     ┌────────▼────┐   ┌──────▼──────┐         │   │
│     │   MySQL     │   │   Redis     │         │   │
│     │   :3306     │   │   :6379     │         │   │
│     └─────────────┘   └─────────────┘         │   │
└─────────────────────────────────────────────────────┘
```

### 1.3 现有 Docker 配置

- ✅ 前端 Dockerfile: `web/Dockerfile`
- ✅ 前端 Nginx 配置: `web/nginx.conf`
- ⚠️ 后端 Dockerfile: 需要创建 `Dockerfile.lazycat`
- ⚠️ Docker Compose: `docker/` 目录下存在但被 gitignore

---

## 二、移植准备清单

### 2.1 需要创建/修改的文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `Dockerfile.backend` | 🆕 新建 | 后端生产镜像 |
| `Dockerfile.frontend` | ✏️ 修改 | 基于 web/Dockerfile 优化 |
| `nginx.lazycat.conf` | 🆕 新建 | 懒猫平台 Nginx 配置 |
| `manifest.yml` | 🆕 新建 | 懒猫微服应用清单 |
| `icon.png` | 🆕 新建 | 应用图标 (256x256) |

### 2.2 镜像准备

需要构建并推送以下镜像：

| 镜像名 | 来源 | 说明 |
|--------|------|------|
| `pet-adoption-backend` | 自建 | Go 后端服务 |
| `pet-adoption-frontend` | 自建 | React 前端 (Nginx) |
| `mysql:8.0` | Docker Hub | MySQL 数据库 |
| `redis:7-alpine` | Docker Hub | Redis 缓存 |

---

## 三、移植步骤

### 阶段一：准备 Docker 镜像

#### 3.1 创建后端 Dockerfile

```dockerfile
# Dockerfile.backend
FROM golang:1.25-alpine AS builder

WORKDIR /app

# 安装依赖
RUN apk add --no-cache git

# 复制依赖文件
COPY go.mod go.sum ./
RUN go mod download

# 复制源码
COPY . .

# 编译
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o server ./cmd/server

# 运行镜像
FROM alpine:3.19

WORKDIR /app

# 安装基础工具
RUN apk add --no-cache ca-certificates tzdata

# 复制编译产物
COPY --from=builder /app/server .
COPY --from=builder /app/config/config.yaml.example ./config/config.yaml

# 创建必要目录
RUN mkdir -p /app/logs /app/uploads

# 设置时区
ENV TZ=Asia/Shanghai

# 暴露端口
EXPOSE 8080

# 启动命令 - 绑定到 0.0.0.0
CMD ["./server"]
```

#### 3.2 优化前端 Dockerfile

```dockerfile
# Dockerfile.frontend (基于 web/Dockerfile)
FROM node:18-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# 运行镜像
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置 (替换默认配置)
COPY nginx.lazycat.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### 3.3 创建 Nginx 配置

```nginx
# nginx.lazycat.conf
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 前端 SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理到后端
    location /api {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查
    location /health {
        proxy_pass http://backend:8080/health;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

### 阶段二：构建并推送镜像

#### 3.4 构建镜像

```bash
# 构建后端镜像 (AMD64)
docker buildx build --platform linux/amd64 \
  -t idocker1688/pet-adoption-backend:1.0.0 \
  -f Dockerfile.backend \
  --push .

# 构建前端镜像 (AMD64)
cd web
docker buildx build --platform linux/amd64 \
  -t idocker1688/pet-adoption-frontend:1.0.0 \
  -f Dockerfile.frontend \
  --push .
```

#### 3.5 复制镜像到懒猫微服镜像库

```bash
# 复制自建镜像
lzc-cli appstore copy-image idocker1688/pet-adoption-backend:1.0.0
lzc-cli appstore copy-image idocker1688/pet-adoption-frontend:1.0.0

# 复制依赖镜像
lzc-cli appstore copy-image mysql:8.0
lzc-cli appstore copy-image redis:7-alpine
```

### 阶段三：创建懒猫微服配置

#### 3.6 创建 manifest.yml

```yaml
package: cloud.lazycat.app.petadoption
version: 1.0.0
name: 爱心宠物领养平台
description: >-
  连接宠物救助机构与领养者的综合性在线平台。支持宠物发布、领养申请、
  机构入驻、社区互动、爱心捐赠等功能。让每一只流浪动物都能找到温暖的家。
author: OLRain
homepage: https://gitee.com/olrain/pet-adoption-platform

locales:
  zh:
    name: 爱心宠物领养平台
    description: 连接宠物救助机构与领养者的综合性在线平台，让每一只流浪动物都能找到温暖的家。
  en:
    name: Pet Adoption Platform
    description: A comprehensive online platform connecting pet rescue organizations with adopters.

application:
  subdomain: petadoption
  multi_instance: false
  background_task: false
  routes:
    - /=http://frontend:80/
    - /api=http://backend:8080/api
    - /health=http://backend:8080/health
  public_path:
    - /
    - /api/v1/users/login
    - /api/v1/users/register
    - /api/v1/pets
    - /api/v1/organizations
  ingress:
    - protocol: tcp
      port: 80
      service: frontend

# 不支持移动端（管理后台复杂表单）
unsupported_platforms:
  - ios
  - android

services:
  # MySQL 数据库
  mysql:
    image: registry.lazycat.cloud/xxx/mysql:8.0  # 替换为实际哈希
    user: root
    environment:
      - MYSQL_ROOT_PASSWORD=PetAdoption@2025
      - MYSQL_DATABASE=pet_adoption
      - MYSQL_USER=pet_user
      - MYSQL_PASSWORD=PetAdoption@2025
    binds:
      - /lzcapp/var/mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 30s
      timeout: 20s
      retries: 10
      start_period: 120s

  # Redis 缓存
  redis:
    image: registry.lazycat.cloud/xxx/redis:7-alpine  # 替换为实际哈希
    binds:
      - /lzcapp/var/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  # Go 后端
  backend:
    image: registry.lazycat.cloud/xxx/pet-adoption-backend:1.0.0  # 替换为实际哈希
    user: root
    depends_on:
      - mysql
      - redis
    environment:
      # 数据库配置
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_USER=pet_user
      - DB_PASSWORD=PetAdoption@2025
      - DB_NAME=pet_adoption
      # Redis 配置
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      # 应用配置
      - SERVER_MODE=release
      - JWT_SECRET=${LAZYCAT_BOX_NAME}
      - BASE_URL=https://${LAZYCAT_APP_DOMAIN}
    binds:
      - /lzcapp/var/logs:/app/logs
      - /lzcapp/var/uploads:/app/uploads
    setup_script: |
      echo "等待 MySQL 启动..."
      for i in $(seq 1 60); do
        nc -z mysql 3306 >/dev/null 2>&1 && break
        echo "等待 MySQL... ($i/60)"
        sleep 2
      done
      
      echo "等待 Redis 启动..."
      for i in $(seq 1 30); do
        nc -z redis 6379 >/dev/null 2>&1 && break
        echo "等待 Redis... ($i/30)"
        sleep 2
      done
      
      echo "依赖服务已就绪，启动应用..."
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # React 前端
  frontend:
    image: registry.lazycat.cloud/xxx/pet-adoption-frontend:1.0.0  # 替换为实际哈希
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
```

#### 3.7 准备应用图标

- 尺寸：256 x 256 像素
- 格式：PNG
- 建议：使用可爱的宠物图标，如爪印、猫狗头像等

### 阶段四：打包与测试

#### 3.8 创建 LPK 包

```bash
# 创建打包目录
mkdir -p lpk-package

# 复制必要文件
cp manifest.yml lpk-package/
cp icon.png lpk-package/

# 打包
cd lpk-package
zip -r ../pet-adoption.lpk manifest.yml icon.png
```

#### 3.9 安装测试

```bash
# 安装应用
lzc-cli app install pet-adoption.lpk

# 查看状态
lzc-cli app status cloud.lazycat.app.petadoption

# 查看日志
lzc-cli app log cloud.lazycat.app.petadoption
```

---

## 四、配置适配要点

### 4.1 后端配置修改

需要修改 `config/config.go` 支持环境变量读取：

```go
// 从环境变量读取配置，支持懒猫微服动态注入
func LoadConfig() {
    // 数据库配置
    dbHost := os.Getenv("DB_HOST")
    if dbHost == "" {
        dbHost = "localhost"
    }
    
    // Redis 配置
    redisHost := os.Getenv("REDIS_HOST")
    if redisHost == "" {
        redisHost = "localhost"
    }
    
    // 动态域名
    baseURL := os.Getenv("BASE_URL")
    // ...
}
```

### 4.2 前端配置修改

在 `web/vite.config.ts` 中配置 API 代理（开发环境）：

```typescript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://backend:8080',
        changeOrigin: true
      }
    }
  }
})
```

### 4.3 CORS 配置

确保后端 CORS 中间件支持动态域名：

```go
func CORSMiddleware() gin.HandlerFunc {
    return func(c *gin.Context) {
        origin := c.GetHeader("Origin")
        // 允许懒猫微服域名
        if strings.Contains(origin, "lazycat") || 
           strings.Contains(origin, "lzcapp") {
            c.Header("Access-Control-Allow-Origin", origin)
        }
        // ...
    }
}
```

---

## 五、验证清单

### 5.1 功能验证

- [ ] 首页正常加载
- [ ] 用户注册/登录正常
- [ ] 宠物列表/详情正常
- [ ] 领养申请流程正常
- [ ] 管理后台可访问
- [ ] 图片上传正常
- [ ] 数据持久化正常（重启后数据不丢失）

### 5.2 性能验证

- [ ] 页面加载时间 < 3秒
- [ ] API 响应时间 < 500ms
- [ ] 无明显内存泄漏

### 5.3 安全验证

- [ ] 敏感信息未硬编码
- [ ] JWT 认证正常
- [ ] 管理员权限隔离正常

---

## 六、常见问题预处理

### 6.1 MySQL 启动慢

MySQL 首次启动需要初始化，`start_period` 设置为 120s。

### 6.2 前端 API 请求 404

检查 Nginx 配置中的 `proxy_pass` 是否使用服务名 `backend`。

### 6.3 CORS 错误

确保后端 CORS 配置允许 `${LAZYCAT_APP_DOMAIN}` 域名。

### 6.4 数据库连接失败

使用 `setup_script` 等待 MySQL 完全就绪后再启动后端。

---

## 七、项目时间表

| 阶段 | 任务 | 预计时间 |
|------|------|----------|
| 1 | 创建 Dockerfile 和配置文件 | 30分钟 |
| 2 | 构建并推送镜像 | 20分钟 |
| 3 | 复制镜像到懒猫微服 | 10分钟 |
| 4 | 创建 manifest.yml | 15分钟 |
| 5 | 打包并安装测试 | 15分钟 |
| 6 | 功能验证与调试 | 30-60分钟 |

**总计**: 约 2-3 小时

---

## 八、参考资源

- [懒猫微服开发文档](https://developer.lazycat.cloud/)
- [Manifest 规范](https://developer.lazycat.cloud/spec/manifest.html)
- [健康检查配置](https://developer.lazycat.cloud/spec/manifest.html#_5-2-healthcheckconfig)
- [开源项目移植到懒猫微服通用指南 v3.4](./开源项目移植到懒猫微服通用指南v3.4.md)

---

**文档版本**: v1.0  
**维护者**: OLRain  
**最后更新**: 2025-12-31
