# 前端测试方案

## 1. 测试策略

### 1.1 测试类型

| 测试类型 | 工具 | 覆盖范围 | 优先级 |
|---------|------|---------|--------|
| 单元测试 | Vitest | 工具函数、Hooks | 高 |
| 组件测试 | React Testing Library | UI组件 | 高 |
| E2E测试 | Playwright | 用户流程 | 中 |
| 视觉测试 | Storybook | UI一致性 | 低 |

### 1.2 测试覆盖率目标

- 工具函数: 90%+
- React Hooks: 80%+
- UI组件: 70%+
- 整体覆盖率: 75%+

---

## 2. 测试环境配置

### 2.1 安装测试依赖

```bash
cd web

# 安装Vitest和测试工具
npm install -D vitest @vitest/ui
npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event
npm install -D jsdom
npm install -D @playwright/test

# 安装Mock工具
npm install -D msw
```

### 2.2 配置Vitest

**文件**: `web/vitest.config.ts`

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      exclude: [
        'node_modules/',
        'src/test/',
        '**/*.d.ts',
        '**/*.config.*',
        '**/mockData',
        'src/main.tsx',
      ],
    },
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

### 2.3 测试设置文件

**文件**: `web/src/test/setup.ts`

```typescript
import '@testing-library/jest-dom'
import { cleanup } from '@testing-library/react'
import { afterEach } from 'vitest'

// 每个测试后清理
afterEach(() => {
  cleanup()
})

// Mock window.matchMedia
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: vi.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn(),
  })),
})

// Mock IntersectionObserver
global.IntersectionObserver = class IntersectionObserver {
  constructor() {}
  disconnect() {}
  observe() {}
  takeRecords() {
    return []
  }
  unobserve() {}
}
```

### 2.4 更新package.json

```json
{
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  }
}
```

---

## 3. 单元测试

### 3.1 工具函数测试

**文件**: `web/src/lib/__tests__/utils.test.ts`

```typescript
import { describe, it, expect } from 'vitest'
import { cn, formatDate, formatPrice } from '../utils'

describe('utils', () => {
  describe('cn', () => {
    it('合并类名', () => {
      expect(cn('foo', 'bar')).toBe('foo bar')
    })

    it('处理条件类名', () => {
      expect(cn('foo', false && 'bar', 'baz')).toBe('foo baz')
    })
  })

  describe('formatDate', () => {
    it('格式化日期', () => {
      const date = new Date('2024-12-17')
      expect(formatDate(date)).toBe('2024-12-17')
    })
  })

  describe('formatPrice', () => {
    it('格式化价格', () => {
      expect(formatPrice(100)).toBe('¥100.00')
      expect(formatPrice(1234.56)).toBe('¥1,234.56')
    })
  })
})
```

### 3.2 API客户端测试

**文件**: `web/src/lib/__tests__/api.test.ts`

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import axios from 'axios'
import { userApi } from '../api'

vi.mock('axios')

describe('userApi', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  describe('login', () => {
    it('成功登录', async () => {
      const mockResponse = {
        data: {
          code: 200,
          data: {
            token: 'test-token',
            user: { id: 1, username: 'testuser' }
          }
        }
      }
      
      vi.mocked(axios.post).mockResolvedValue(mockResponse)

      const result = await userApi.login({
        username: 'testuser',
        password: 'password'
      })

      expect(result.data.token).toBe('test-token')
      expect(axios.post).toHaveBeenCalledWith(
        '/api/v1/users/login',
        { username: 'testuser', password: 'password' }
      )
    })

    it('登录失败', async () => {
      vi.mocked(axios.post).mockRejectedValue(
        new Error('Invalid credentials')
      )

      await expect(
        userApi.login({ username: 'test', password: 'wrong' })
      ).rejects.toThrow('Invalid credentials')
    })
  })
})
```

### 3.3 Zustand Store测试

**文件**: `web/src/store/__tests__/auth.test.ts`

```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { useAuthStore } from '../auth'

describe('authStore', () => {
  beforeEach(() => {
    // 重置store
    useAuthStore.setState({
      user: null,
      token: null,
      isAuthenticated: false
    })
  })

  it('初始状态', () => {
    const state = useAuthStore.getState()
    expect(state.user).toBeNull()
    expect(state.token).toBeNull()
    expect(state.isAuthenticated).toBe(false)
  })

  it('登录成功', async () => {
    const mockUser = { id: 1, username: 'testuser' }
    const mockToken = 'test-token'

    // 模拟登录
    useAuthStore.setState({
      user: mockUser,
      token: mockToken,
      isAuthenticated: true
    })

    const state = useAuthStore.getState()
    expect(state.user).toEqual(mockUser)
    expect(state.token).toBe(mockToken)
    expect(state.isAuthenticated).toBe(true)
  })

  it('登出', () => {
    // 先设置登录状态
    useAuthStore.setState({
      user: { id: 1, username: 'test' },
      token: 'token',
      isAuthenticated: true
    })

    // 执行登出
    useAuthStore.getState().logout()

    const state = useAuthStore.getState()
    expect(state.user).toBeNull()
    expect(state.token).toBeNull()
    expect(state.isAuthenticated).toBe(false)
  })
})
```

---

## 4. 组件测试

### 4.1 UI组件测试

**文件**: `web/src/components/ui/__tests__/Button.test.tsx`

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { Button } from '../button'

describe('Button', () => {
  it('渲染按钮', () => {
    render(<Button>Click me</Button>)
    expect(screen.getByText('Click me')).toBeInTheDocument()
  })

  it('处理点击事件', () => {
    const handleClick = vi.fn()
    render(<Button onClick={handleClick}>Click me</Button>)
    
    fireEvent.click(screen.getByText('Click me'))
    expect(handleClick).toHaveBeenCalledTimes(1)
  })

  it('禁用状态', () => {
    render(<Button disabled>Disabled</Button>)
    const button = screen.getByText('Disabled')
    expect(button).toBeDisabled()
  })

  it('不同变体', () => {
    const { rerender } = render(<Button variant="default">Default</Button>)
    expect(screen.getByText('Default')).toHaveClass('bg-primary')

    rerender(<Button variant="destructive">Destructive</Button>)
    expect(screen.getByText('Destructive')).toHaveClass('bg-destructive')
  })
})
```

### 4.2 表单组件测试

**文件**: `web/src/components/__tests__/LoginForm.test.tsx`

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { LoginForm } from '../LoginForm'

describe('LoginForm', () => {
  it('渲染登录表单', () => {
    render(<LoginForm />)
    
    expect(screen.getByLabelText(/用户名/i)).toBeInTheDocument()
    expect(screen.getByLabelText(/密码/i)).toBeInTheDocument()
    expect(screen.getByRole('button', { name: /登录/i })).toBeInTheDocument()
  })

  it('验证必填字段', async () => {
    render(<LoginForm />)
    
    const submitButton = screen.getByRole('button', { name: /登录/i })
    fireEvent.click(submitButton)

    await waitFor(() => {
      expect(screen.getByText(/请输入用户名/i)).toBeInTheDocument()
      expect(screen.getByText(/请输入密码/i)).toBeInTheDocument()
    })
  })

  it('提交表单', async () => {
    const handleSubmit = vi.fn()
    render(<LoginForm onSubmit={handleSubmit} />)

    const user = userEvent.setup()
    
    await user.type(screen.getByLabelText(/用户名/i), 'testuser')
    await user.type(screen.getByLabelText(/密码/i), 'password123')
    await user.click(screen.getByRole('button', { name: /登录/i }))

    await waitFor(() => {
      expect(handleSubmit).toHaveBeenCalledWith({
        username: 'testuser',
        password: 'password123'
      })
    })
  })
})
```

### 4.3 页面组件测试

**文件**: `web/src/pages/__tests__/PetList.test.tsx`

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import { BrowserRouter } from 'react-router-dom'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { PetList } from '../PetList'
import * as api from '@/lib/api'

vi.mock('@/lib/api')

const queryClient = new QueryClient({
  defaultOptions: {
    queries: { retry: false }
  }
})

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <BrowserRouter>
    <QueryClientProvider client={queryClient}>
      {children}
    </QueryClientProvider>
  </BrowserRouter>
)

describe('PetList', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('显示加载状态', () => {
    vi.mocked(api.petApi.list).mockImplementation(
      () => new Promise(() => {}) // 永不resolve
    )

    render(<PetList />, { wrapper })
    expect(screen.getByText(/加载中/i)).toBeInTheDocument()
  })

  it('显示宠物列表', async () => {
    const mockPets = [
      { id: 1, name: '小白', type: 'dog', breed: '金毛' },
      { id: 2, name: '小黑', type: 'cat', breed: '英短' }
    ]

    vi.mocked(api.petApi.list).mockResolvedValue({
      data: { code: 200, data: mockPets }
    })

    render(<PetList />, { wrapper })

    await waitFor(() => {
      expect(screen.getByText('小白')).toBeInTheDocument()
      expect(screen.getByText('小黑')).toBeInTheDocument()
    })
  })

  it('显示错误信息', async () => {
    vi.mocked(api.petApi.list).mockRejectedValue(
      new Error('Failed to fetch')
    )

    render(<PetList />, { wrapper })

    await waitFor(() => {
      expect(screen.getByText(/加载失败/i)).toBeInTheDocument()
    })
  })
})
```

---

## 5. E2E测试

### 5.1 配置Playwright

**文件**: `web/playwright.config.ts`

```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },

  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
  ],

  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
})
```

### 5.2 用户登录流程测试

**文件**: `web/e2e/auth.spec.ts`

```typescript
import { test, expect } from '@playwright/test'

test.describe('用户认证', () => {
  test('用户登录流程', async ({ page }) => {
    // 访问登录页
    await page.goto('/login')

    // 填写表单
    await page.fill('input[name="username"]', 'testuser')
    await page.fill('input[name="password"]', 'Test@123456')

    // 点击登录按钮
    await page.click('button[type="submit"]')

    // 验证跳转到首页
    await expect(page).toHaveURL('/')

    // 验证显示用户信息
    await expect(page.locator('text=testuser')).toBeVisible()
  })

  test('登录失败提示', async ({ page }) => {
    await page.goto('/login')

    await page.fill('input[name="username"]', 'wronguser')
    await page.fill('input[name="password"]', 'wrongpass')
    await page.click('button[type="submit"]')

    // 验证错误提示
    await expect(page.locator('text=/用户名或密码错误/i')).toBeVisible()
  })

  test('用户注册流程', async ({ page }) => {
    await page.goto('/register')

    await page.fill('input[name="username"]', 'newuser')
    await page.fill('input[name="password"]', 'Test@123456')
    await page.fill('input[name="phone"]', '13800138000')
    await page.fill('input[name="email"]', 'new@example.com')

    await page.click('button[type="submit"]')

    // 验证注册成功提示
    await expect(page.locator('text=/注册成功/i')).toBeVisible()
  })
})
```

### 5.3 宠物浏览流程测试

**文件**: `web/e2e/pet.spec.ts`

```typescript
import { test, expect } from '@playwright/test'

test.describe('宠物浏览', () => {
  test('浏览宠物列表', async ({ page }) => {
    await page.goto('/pets')

    // 验证列表加载
    await expect(page.locator('.pet-card')).toHaveCount(10)

    // 验证筛选功能
    await page.selectOption('select[name="type"]', 'dog')
    await page.click('button:has-text("搜索")')

    // 验证筛选结果
    await expect(page.locator('.pet-card')).toHaveCount(5)
  })

  test('查看宠物详情', async ({ page }) => {
    await page.goto('/pets')

    // 点击第一个宠物
    await page.click('.pet-card:first-child')

    // 验证详情页
    await expect(page).toHaveURL(/\/pets\/\d+/)
    await expect(page.locator('h1')).toBeVisible()
    await expect(page.locator('text=/品种/i')).toBeVisible()
  })

  test('搜索宠物', async ({ page }) => {
    await page.goto('/pets')

    // 输入搜索关键词
    await page.fill('input[placeholder*="搜索"]', '金毛')
    await page.press('input[placeholder*="搜索"]', 'Enter')

    // 验证搜索结果
    await expect(page.locator('text=/金毛/i')).toBeVisible()
  })
})
```

### 5.4 领养申请流程测试

**文件**: `web/e2e/adoption.spec.ts`

```typescript
import { test, expect } from '@playwright/test'

test.describe('领养申请', () => {
  test.beforeEach(async ({ page }) => {
    // 登录
    await page.goto('/login')
    await page.fill('input[name="username"]', 'testuser')
    await page.fill('input[name="password"]', 'Test@123456')
    await page.click('button[type="submit"]')
    await page.waitForURL('/')
  })

  test('提交领养申请', async ({ page }) => {
    // 进入宠物详情页
    await page.goto('/pets/1')

    // 点击申请领养
    await page.click('button:has-text("申请领养")')

    // 填写申请表单
    await page.fill('input[name="applicant_name"]', '张三')
    await page.fill('input[name="applicant_phone"]', '13800138000')
    await page.fill('textarea[name="adoption_reason"]', '喜欢宠物')
    await page.fill('textarea[name="how_to_care"]', '每天遛狗')

    // 提交申请
    await page.click('button[type="submit"]')

    // 验证成功提示
    await expect(page.locator('text=/申请提交成功/i')).toBeVisible()
  })

  test('查看我的申请', async ({ page }) => {
    await page.goto('/my-applications')

    // 验证申请列表
    await expect(page.locator('.application-card')).toHaveCount.greaterThan(0)

    // 点击查看详情
    await page.click('.application-card:first-child button:has-text("查看详情")')

    // 验证详情显示
    await expect(page.locator('text=/申请状态/i')).toBeVisible()
  })
})
```

---

## 6. 测试命令

### 6.1 运行测试

```bash
# 单元测试和组件测试
npm test                    # 监听模式
npm run test:run           # 运行一次
npm run test:ui            # UI模式
npm run test:coverage      # 生成覆盖率报告

# E2E测试
npm run test:e2e           # 运行E2E测试
npm run test:e2e:ui        # UI模式运行E2E测试
```

### 6.2 CI/CD集成

**文件**: `.github/workflows/frontend-test.yml`

```yaml
name: Frontend Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json
      
      - name: Install dependencies
        working-directory: ./web
        run: npm ci
      
      - name: Run tests
        working-directory: ./web
        run: npm run test:run
      
      - name: Generate coverage
        working-directory: ./web
        run: npm run test:coverage
      
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./web/coverage/coverage-final.json
```

---

## 7. 测试最佳实践

### 7.1 测试原则

1. **测试行为，不是实现**
   - 关注用户交互和结果
   - 避免测试内部实现细节

2. **保持测试简单**
   - 一个测试只验证一个功能点
   - 使用清晰的测试描述

3. **使用真实场景**
   - 模拟真实用户操作
   - 使用真实的数据结构

4. **避免测试耦合**
   - 测试之间相互独立
   - 每个测试都能单独运行

### 7.2 Mock策略

```typescript
// 好的Mock - 只Mock外部依赖
vi.mock('@/lib/api', () => ({
  userApi: {
    login: vi.fn()
  }
}))

// 避免 - 过度Mock内部逻辑
// 不要Mock组件内部的函数
```

### 7.3 测试覆盖率

```bash
# 查看覆盖率报告
npm run test:coverage

# 在浏览器中查看详细报告
open coverage/index.html
```

---

**文档版本**: v1.0  
**创建日期**: 2024-12-17  
**最后更新**: 2024-12-17
